# 51 · 收口清单 —— 第零步之前所有诉求，一条一条过

> 2026-09-14。作品负责人："这是我们做第零步之前最后一个需求了，把之前所有的需求都统筹成一个 checklist 去过，
> 做完、收尾、合到 main、整体没问题了，再往下移。"
>
> 规矩：每一条写**谁提的诉求（原话要点）→ 落在哪 → 怎么验的 → 状态**。
> 状态只有四种：✅ 做完且验过 · 🟡 做完但只在无头环境验过（缺真人 / 真摄像头 / 真 GPU）· ⏳ 在跑 · ⛔ 明确不做或暂缓（写理由）。
> 最后一次整体验证的结果写在文末 §Z，没有 §Z 的那一刻这份清单就还没收口。

## A · 舞台上的字和面板

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| A1 | 左下读数：灰色半透明、无边、数字不放大、单位小一号贴基线、默认收起、底条「显示/收起」 | `ui/readout.{ts,css}` · docs/26 §F | `test/readout.test.ts`（35 条）；五场景截图 | ✅ |
| A2 | 读数像数控：越界变色、告警代码、不闪 | `ui/readout-state.ts` `assess` / `createAlarmWatch` | 同上（阈值复用、滞回、开机不误报） | ✅ |
| A3 | 没人时不许说「有人」 | 回放时写「摄像头关着」、全破折号、不报警 | 读数测试先红后绿；截图 | ✅ |
| A4 | 上半截更透、偏灰；两端往中间靠；深空单独调；底条和上半截拉开 | `readout.css` 文件头第二节 4–6 条 | 按采样器回差带扫纯黑→纯白每一档 ≥4.5:1；告警色 ≥3:1；区间 0.62–0.80、两侧差 ≤0.1 | ✅（负责人本地看过深空之外全部场景） |
| A5 | 四角文字随场景明暗自适应 | `stage/ink-regions.ts` / `ink-sampler.ts` | 对比度测试；五场景截图 | ✅ |
| A6 | 描边像画出来的、淡一点稀疏一点 | `TOON.outlineWeightVariation` | 截图 | ✅ |

## B · 右上角控件

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| B1 | 哪些默认开：生命力开、描边开…… | docs/23 §S4.1 默认值表 | `test/control-table.test.ts` 对 `readFlags('')` | ✅ |
| B2 | 点按钮是叠加或开关，不许控死 | `shell/intent.ts` · `ui/control-table.ts` | `test/intent.test.ts`（叠加时弧线照走，拿掉即复原） | ✅ |
| B3 | 面板保留/显示/删什么；架构优雅、可扩展 | 一张控件表推出面板、键、URL、随机、回程白名单 | docs/23 §S4.1「怎么加一个控件」 | ✅ |
| B4 | 面板全透明；选中和其它状态怎么让人意识到 | 状态表（线形 + 等宽小字，不只靠颜色） | `test/controls-surface.test.ts` | ✅ |
| B5 | 删掉「叠加 · 再点撤销」文字提示 | `controls.ts` | 测试防回潮 | ✅ |
| B6 | 面板不压右下角出口 | `--sb-exits-h` + 面板自量顶边 | Chrome 实测 0px 重叠 | ✅ |

## C · 页面、入口、出口、过场

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| C1 | 工作台顶上有直观的退出/返回 | `dev/devnav.ts` | 截图、Escape | ✅ |
| C2 | 有价值的页面从正文超链接引出，回得来 | `ui/return-to.ts` | 无头逐页点过 | ✅ |
| C3 | /dev/ 目录里的链接、没有出口的页 | 每页至少一个出口；房间页加目录；`?from=/dev` 回工作台；404 页 | `test/page-exits.test.ts`；两次无头审计（dev 5741 / 构建 4173 各 19 条全通） | ✅ |
| C4 | 页面切换迟钝、生硬：颜色、动效、共享元素平移 | `ui/transitions.ts`；首帧底色、选卡→舞台交接 | docs/47 前后数字；27 跳 0 白帧 | 🟡 **已开**：白屏根因是 WebGPU 画布在快照里是透明的 + 叠层缺省白 → 快照前把画布冻成图、叠层铺底色；字标/标题/目录挪位、深浅底交叉淡入、舞台→大厅淡出、页脚与工作台出口条跨页不动。防护：不支持 / 减少动画 / `?vt=off` / 现场 / 前进后退 / 摄像头在的页 / 出错 / 630ms 超时 → 直接切。有头 Chrome 修后 9 轮，最后 2 轮 30 跳 0 白；摄像头舞台离开与 404 仍白 → 这两类直接切。没做完的记在 docs/53 Q4 |
| C6 | 页脚那枚 SEE-ME SEE-U 示意图，统一放在页面最下面 | `ui/footer-mark.{ts,css,svg}`：陈述 / 谱系 / 做的过程 / 护照 / 工作台目录 + 三个侧室；viewBox 贴墨裁、四边匀 1.5；蒙版 + `--sb-ink` 着色；不支持蒙版不画 | `test/footer-mark.test.ts`（先红）；8 页无头截图：每页一枚、是最后一个元素、宽 1267、墨色与正文同 | ✅ |
| C7 | 同组几页右上角按钮：中英挨得太近、按钮间距不好看；「退出 · 回到作品」太笼统 | `dev/devnav.css`：中英 0.55em（原 0px —— 链接被 `.sb-bi` 算回 block，flex gap 失效）、出口之间 2.4em；文案改「回到作品」 | 逐页量 zh↔en 间距（无头 CDP）；`test/devnav-spacing.test.ts`（先红）；/dev、/dev/figure、/dev/lineup 截图 | ✅ |
| C8 | /dev 目录：整页太紧、和「作品」挨着；正文里露出 `**`；logo 没放大、不是那枚字体 | 页头与第一组之间 2.5 栏距；侧室那句去掉 markdown 星号与已失效的「不在目录里」；左上换成两行字标（h1 档） | 截图 | ✅ |
| C9 | 页脚示意图：三轮调整后由维护者定稿（负责人："自己做完决定就执行"） | 挂回**页面那一栏**（不是 body）、吃满栏宽、墨用 `.sb-diagram--flush` 和正文字左对齐；上五倍栏距、下半个安全区 | 8 页截图：墨左缘 /about x≈60、/dev x≈48，与各页正文同线；每页一枚、是最后一个元素；`test/footer-mark.test.ts` 防挂回 body | ✅ |
| C10 | /about 巨题换成示意图 | 试过后负责人**撤回**：巨题回到原来的字形、字体和 `ed-rise` 动效 | 截图；测试钉住巨题仍是 `sb-display ed-rise`、不含示意图 | ⛔ 撤回（负责人决定） |
| C5 | 导航审计、轻量改；大决策自己做 | docs/47 审计表；改了哪些主页面列在 docs/47 | 同上 | ✅ |

## D · 流畅度与防护

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| D1 | 开摄像头先黑屏再卡 | 推理进 worker；录像撑到第一次识别；「正在打开」 | docs/48：主线程冻结 4 次/939ms → 0 | 🟡 |
| D2 | 大动作时卡 | 识别间插值；dt 截断 | 大动作 >25ms 帧 29 → 7 | 🟡 |
| D3 | 整页防卡：一般化治理、恢复、及时反馈 | `shell/governor.ts` 六级阶梯；device lost / 摄像头断开恢复 | 阶梯/回差/恢复测试；每级对应真开关 | 🟡 |
| D4 | 高 DPI 默认像素比 | `GOVERNOR.dprMax = 1.5` | `test/dpr-cap.test.ts` | ✅ |
| D5 | 弧线 30–47s 换件段长任务；调速器切换本身的顿帧 | 空闲时预编译直出管线（`stage/warm-plan.ts`）；镜像件空闲分片（`assets/idle-queue.ts`） | docs/48 §10；首次关后期 213–535ms → 半数 <100ms | ⛔ 线已冻结（负责人）：拿回后期的顿、GPU 管线长帧没修 —— docs/53 Q2 Q3 |
| D6 | 帧预算（draw ≤40 / 面 ≤250k） | `creature/swap-budget.ts` | `test/swap-budget.test.ts` | ✅ |
| D7 | 展签 → 选择页 → 舞台 → 按「摄像头」约 2.3 秒后整页不再出帧（性能线查出，有窗口的 Chrome 也复现，JS 还活着） | 兜底：`shell/stall.ts` 定时器看门（不靠 rAF），可见页面 4 秒无帧 → `degradeTo('reload')`（每会话最多两次） | `test/stall-watchdog.test.ts`（先红）；复现步骤与线索在 docs/48 §10.6 | 🟡 **根因没追到**：只做了恢复，没修冻结本身；下一步是关掉选择页→舞台的交棒再复现 |

## E · 取景与多人

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| E1 | Auto framing 调研：叫什么、开源怎么做、合不合适 | docs/49 §1–4 | 一手来源标注 | ✅ |
| E2 | 只露上半身 → 下半身不动、小范围跟随；退后 → 切全景；两类人实时切换、实时可看 | `core/src/autoframe.ts` / `leghold.ts`；`/dev/framing.html`；`?framing=` | 22 core + 7 app 测试；21 处变异全红；时间线回放 | 🟡 没有真人坐在笔记本前试过 |
| E3 | 装置开场检查：关掉系统/摄像头自带的自动取景 | docs/38、docs/46 | — | ✅ |
| E4 | 两三个人入镜，每人一台机器人；上限；性能、缓存、UI/UX、边界 | `core/src/people.ts` 身份跟踪；同伴共用实例桶；`?people=2\|3`、控件、调速器一级；`/dev/people.html`；docs/50；拖动交互计划 docs/52 | core 276 / app 531；21 条守卫逐条先红；无头 1 vs 2 具 p95 12.0 / 10.7ms | 🟡 线上默认仍 1 人（推理耗时表没跑）；第三具要 LOD；拖动只有计划 —— docs/53 Q5 Q6 Q8 Q9 |

## F · 身体、弧线、零件

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| F1 | 忒修斯替换、一条线、没有乐章边界 | docs/44；`sound/timbre.ts`；tier 由排期器给 | arc-edges / sound-line / first-replacement 测试 | ✅ |
| F2 | 真实机器几何（athlete / wheelleg / manipulator） | docs/42 §8 | check:parts 247 件 0 错；授权文本 | ✅（manipulator 不像 Stretch、wheelleg 侧面头飘：要改布局，⛔ 暂缓） |
| F3 | 交接正中间描边瘪一半 | 旧分支 `a0198985` | — | ⛔ 暂缓，docs/44 §11 写了理由 |
| F4 | tier-3 跨主题件绕过借件预算门 | — | 发现未修 | ⛔ 暂缓（基因组那一块） |

## G · 线上、存档、隐私、发布

| # | 诉求 | 落在哪 | 怎么验 | 状态 |
|---|---|---|---|---|
| G1 | 线上存档不靠 Vercel 权限、免费 | Cloudflare Worker + D1 | `GET /visits` → `{ok,total,entries}`；未写测试行 | ✅ |
| G2 | 访客剪影、本地存档不进构建 | `build/ship-filter.ts` | 测试 + 故意放文件的真构建 | ✅ |
| G3 | 黑客松时间窗只做语境，不写到线上 | 线上不写比赛起止时间；/making 的数字只报提交时刻（第一条 / 最后一条），不报时长 | grep 构建产物 | ✅（提交时间戳是做的过程的记录，不是比赛时间窗） |
| G4 | 上线检查单 docs/13 | 首屏、无权限、无 WebGPU、raw、隐私、存档 | 每次合并后重跑 | 见 §Z |
| G5 | 做完的都在 main、线上是这一版 | — | HEAD = origin/main；线上入口 = 本地构建 | 见 §Z |

## H · 仓库卫生（不急，第一步再过）

| # | 事项 | 状态 |
|---|---|---|
| H1 | 6 条会话额度中断留下的半成品分支（记号页、选择页、手势、角落导航、被看见、导出静帧） | ⛔ 第一步逐个判断捡回还是丢 |
| H2 | `assets/refs/` 下 5 个未提交的锚定图文件夹，其中 guest.keynote 可能是可辨认的真人 | ⛔ 等负责人确认，不提交 |
| H3 | 已合并的子代理 worktree 本地残留 | 收口时清理已合并且无改动的 |

## Z · 最后一次整体验证

2026-09-14 夜，四条线（性能、多人、跨页过场，加主线的页脚 / 工作台 / 读数 / 看门狗）全部合进 main 之后：

| 项 | 结果 |
|---|---|
| commit | `7c23959`（Merge: cross-page transitions on with safeguards），`main` = `origin/main` |
| `npm run check` | core 276 / 0 失败，app 540 / 0 失败，check:parts 247 件 0 错 12 警告 |
| `npm run build` | 通过 |
| docs/13 上线检查单（无头 Chrome，本地构建） | 首屏 95 个请求 2.08 MB（< 3 MB）；拒绝摄像头权限：不白屏、0 错误；去掉 WebGPU：到得了选择页、0 错误 |
| 线上入口 | `useeme.ptoq.io` 服务 `main-97tczYR5.js`，与本地构建逐字相同 |
| 存档 Worker | `GET /visits` → `{ok:true,total:0,entries:[]}`（没写测试行） |
| 没验的 | 真人、真摄像头、真显卡；Safari / Firefox；30 分钟长跑；多人与 Auto framing 的真人场景 |

**收口结论**：负责人列过的诉求里，✅ 的都做完且验过；🟡 的做完但缺真人 / 真屏证据；⛔ 的是负责人冻结或撤回的。
所有没解决的事都在 [`53-BACKLOG.md`](53-BACKLOG.md)，按 P0–P3 排好 —— **下次从 Q1（摄像头路径冻结的根因）开始**。
