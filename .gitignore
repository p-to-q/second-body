# 两行都要：`node_modules/` 只匹配目录，匹配不到一个**同名的符号链接**。
# 这不是理论问题 —— 工作树里为了共用依赖建的那条软链，被一次 `git add -A`
# 提交进了仓库根（120000 blob），别人 clone 下来就是一条指向仓库外的死链。
node_modules/
node_modules
dist/
.env
.env.*
!.env.example
assets/raw/*/
!assets/raw/ledger.json
scratch/

# 由 vite 插件在 dev/build 时扫 assets/demo/ 生成，不该手写也不该提交
assets/demo/index.json

# 慢回路的血统池：**这台装置**现场长出来的件与它的索引（docs/17）。
# 不提交 —— 它是这台机器的记忆，不是仓库的内容；clone 下来应该是空的，
# 然后由站到摄像头前面的人重新长出来。build 时 shipAssets 会从本地盘复制进 dist。
assets/parts/lineage/
*.log
.DS_Store

# agent worktrees（每个都是独立 git 仓库，绝不能被当成 submodule 提交进来）
.claude/worktrees/
.vercel

# 参考图原料入口 —— 不进 dist（vite 的 shipAssets 只复制 SHIPPED 里那几个目录）
assets/intake/**/*.jpg
assets/intake/**/*.jpeg
assets/intake/**/*.png

# 开源真实 3D 资产的取件区（docs/33）。下载的原始网格不进版本库：
# 体积大，而且各来源授权不同 —— 仓库里只留 scripts/harvest.mjs 和 docs/33，照着能重新取一遍。
assets/harvest/
assets/parts/harvest/

# 真人录制的原料视频（docs/11 T-16，来源见 assets/demo/SOURCES.md）。
# 不进仓库：体积大，各来源授权不同 —— 只留 SOURCES.md，照着能重新取一遍。
# 进仓库的是跑出来的 assets/demo/pose-*.json，那才是"录制"。
assets/capture-src/
archive/
