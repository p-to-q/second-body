# assets/sound — 声音素材与授权

> 完整的声音设计见 `docs/29-SOUND.md`。这一份只管一件事：**这里的每一个文件从哪来、
> 谁做的、什么授权、我们动过它什么。** 字体那边已经这么较真了（`assets/fonts/`），
> 声音没有理由不一样。

## 现状：**九个文件，共 18.9KB**（19,326 字节）

四层**连续**声音仍然全部由 Web Audio 合成（`packages/app/src/sound/`），一个字节素材都没有。
九个文件分属两层，它们不是那四层的素材版，是另一类东西 —— 为什么这两件事不冲突，见下一节。

| 层 | 文件 | 字节 |
|---|---|---|
| 第五层 · 离散接触音（`sound/cues.ts`） | `enter` `pass` `commit` `idle` `reveal` `ground` | 12,875 |
| 第六层 · 工作声（`sound/work.ts` 排程，`cues.ts` 放） | `work-a` `work-b` `work-c` | 6,291 |

前一轮四个共 8,344 字节，这一轮加了五个共 **10,982 字节**。
总量 18.9KB，离"不超过 3MB"还有 **155 倍**的余量 —— 这个数是 `ls -l` 加出来的，
`test/sound.test.ts` 里另有一条断言把这一层钉在"几十 KB"那一档（< 64KB）。
钉的不是那个 3MB 上限（离它还有两个数量级），钉的是**量级本身**：
这一层一旦开始按分钟计，它就不再是接触音了，而没有人会因为总量从 19KB 涨到 800KB 而察觉。

## 为什么合成而不是下载（这是一次判断，不是偷懒）

去 freesound / archive.org 找 CC0 的 room tone 和 drone 是完全可行的，
也确实有大量可用素材（候选见文末）。不用它们，有四条各自独立的理由：

1. **素材是录下来的别处，合成是这里。** 一段录音里有它自己的房间、它自己的空调、
   它自己的那一天。这件作品的第一层要说的是"**这个**房间是活的"。
   借来的房间会和现场的房间打架 —— 而且观众听不出是为什么，只会觉得哪里不对。

2. **素材没有信号入口。** 这四层的全部意义是绑在 `Presence` / `MotionFeatures` /
   `tierChanged` / 慢回路 phase 上（PRD §3「因果可见」）。一段 wav 只能被
   调音量和调滤波；而合成的带通可以让**共振频率跟着物种走**、让带宽跟着玩法走。
   拿素材做同样的事，最后也是在素材上套一层合成处理 —— 那不如直接合成。

3. **23 个条目。** 名册还在长（docs/14）。每加一个物种就要有人去找一段新素材、
   记一次授权、再进一次 3MB 预算，这条流程一定会在某个物种上断掉。
   `SOUND.voice` 是从 `ThemeDef.axes` 推出来的，加物种不需要加任何东西。

4. **循环会被听出来。** 装置要连续跑 30 分钟以上（docs/00 §5）。
   一段 30 秒的底噪素材循环 60 次，站得久一点的观众一定能听出周期。
   合成的噪声用连续的滤波噪声 + 无循环相位的慢呼吸，没有周期可听。

## 为什么第五层反过来用素材（上面那四条**没有**被推翻）

上面四条论证的对象是**连续的氛围层**：一直在响的房间底噪、跟着速度走的身体噪声、
慢回路那段拍频。第五层是另一类东西 —— **离散的接触音**：观众伸手做了一件事，
一记，60–200ms，放完就没了。逐条对一遍，那四条**没有一条落在它身上**：

1. **"借来的房间会和现场的房间打架"** —— 只在声音**一直响着**的时候成立。
   一记 80ms 的敲击，它的尾巴短到根本来不及暴露自己的房间；
   而且我们把每一段的尾巴都掐掉了（见下表"我们做了什么处理"）。
   反过来，恰恰是一次**真实碰撞**里的那一千个微结构（初始接触的毛刺、
   材料的不均匀、手的偶然角度）合成补不出来 —— 合成的敲击听起来永远像
   "一个包络乘一段噪声"，因为它就是。

2. **"素材没有信号入口"** —— 第五层**不需要**信号入口，它绑的不是信号是**动作**。
   `SOUND.cues` 里能调的四个增益 + 一道低通就是它全部的表达，
   再多的连续调制对一记 80ms 的声音没有意义（观众来不及听见它在变）。

3. **"23 个条目，名册还在长"** —— 第五层与物种**无关**。
   这几记是界面与身体的触感，不是**那个物种**的声音；加一个物种不需要加任何一个文件。
   文件数量**不随名册增长**，而且刻意不满（升档、到货、自检、降级全都没有，
   理由在 `tuning.ts` 的 `SOUND.cues` 注释里）。
   （原文写的是"封顶的四个"。这一轮加到了六记 + 第六层的三个变体，
   所以那句话改成了它真正想说的那一条：**不随物种数增长**。
   把"四"写进理由里，是把一个当时的事实当成了一条原则。）

4. **"循环会被听出来"** —— 不循环。每一记是一次性的 `start()`，放完就断开。
   唯一会连着响的是"卡片经过"，那一条靠 `passMinGapMs` 节流、
   靠 `passDetune` 微抖播放速率、靠 `passRepeatDecay` 逐次变轻，
   所以一串滑动听起来是"渐远"，不是同一个采样连放十次。

一句话：**合成擅长连续，录音擅长接触。** 这不是立场反复，是分工。

**唯一一件合成做不了的事**（就连续层而言）：真实空间里那种复杂的早期反射（一个真正的大厅的"味道"）。
如果哪天现场需要那个味道，正确的做法是加**一段 CC0 的脉冲响应（IR）**
走 `ConvolverNode`，而不是换成素材播放 —— 一个 IR 是几十 KB，
而且它给所有四层同时上色，不用为每一层各找一段素材。那时就往下面这张表里加一行。

## 文件清单

> 加任何一个文件都必须在这里补一行，**七列一列都不能少**。
> 没有来源 URL 的文件不许进仓库 —— 现在说不清楚，半年后只会更说不清楚。
> 第七列「用在哪个事件」是后加的：前六列回答"这个文件是哪来的"，
> 第七列回答"**它为什么在这儿**"。没有第七列的话，半年后没人敢删任何一个文件。

| 文件 | 来源 URL | 授权 | 作者 | 我们做了什么处理 | 加入日期 | 用在哪个事件 |
|---|---|---|---|---|---|---|
| `enter.webm` | <https://freesound.org/people/BaggoNotes/sounds/704260/> | CC0 1.0 | BaggoNotes（"Mug_Tap.wav"，铅笔石墨尖敲玻璃咖啡杯，Warm Audio 45 话筒） | 取 128kbps ogg 预览件 → 转单声道 48k → 从起音前 4ms 截 **80ms** → 70Hz 高通去隆隆 → 2ms 淡入 / cos² 淡出 → 峰值归一到 −3dBFS → libopus 64k VBR | 2026-09-13 | **S1 入口「开始」那一下**。同时是 AudioContext 的解锁时刻 |
| `pass.webm` | <https://freesound.org/people/brokenmachinery/sounds/730078/> | CC0 1.0 | brokenmachinery（"paper-slide"，作者原话：sliding a piece of paper, a gentle brushing type sound） | 同上，截 **120ms**（取中段那一片连续摩擦，**不取**任何一个起音峰） | 2026-09-13 | **S2 卡片经过**（滚动 / 拖动 / 键盘跨过一张） |
| `commit.webm` | <https://freesound.org/people/Novablox/sounds/616330/> | CC0 1.0 | Novablox（"Mug Down.mp3"，杯子放到台面上） | 同上，截 **180ms** | 2026-09-13 | **S2 手动确认选中** |
| `idle.webm` | <https://freesound.org/people/JapanYoshiTheGamer/sounds/697777/> | CC0 1.0 | JapanYoshiTheGamer（"3 soft door knocks, modern apartment, wooden #04"，木门轻叩，21 条 take 之一） | 同上，截 **150ms**（只留三记里的**第一记**） | 2026-09-13 | **S2 30 秒无操作自动选择**。运行时还额外过一道 `SOUND.cues.idleTilt` = 600Hz 低通 —— 那是"更远"，不烘进文件 |
| `reveal.webm` | <https://freesound.org/people/jaeger2801/sounds/559286/> | CC0 1.0 | jaeger2801（"open a wood box.wav"，打开一只木盒） | 同上，从 **0.120s** 起截 **170ms**（取盒盖那一记接触，**避开**前面 0.06/0.13 那一对连着的开闩声） | 2026-09-14 | **S2 选择页落定**。触发点还没接上，见 docs/29 §2.8 |
| `ground.webm` | <https://freesound.org/people/VizAion/sounds/795634/> | CC0 1.0 | VizAion（"Wood, Hit, Inside Piano, Very Low"，敲钢琴内部的木体，很低） | 同上，从 **0.020s** 起截 **200ms** | 2026-09-14 | **S4 肢体触地**。触发点还没接上，见 docs/29 §2.8 |
| `work-a.webm` | <https://freesound.org/people/shanecutrufello/sounds/556302/> | CC0 1.0 | shanecutrufello（"Audio 01\_08 (wood box)-01.wav"，磁吸木盒啪一下合上） | 同上，从 **0** 起截 **140ms**（整段本来就只有一记，干净的单次指数衰减） | 2026-09-14 | **S6 慢回路在跑**（第六层三个变体之一）。运行时过一道 `WORK.tilt` = 900Hz 低通 |
| `work-b.webm` | <https://freesound.org/people/mrh4hn/sounds/426631/> | CC0 1.0 | mrh4hn（"Picking up and Putting Down Object.wav"，把一件东西拿起来又放下） | 同上，从 **6.235s** 起截 **120ms**（整段 6.8 秒里只有这一记是干净的单起音；6.165s 那一记后面 75ms 还有第二下，**没要**） | 2026-09-14 | 同上（变体 b） |
| `work-c.webm` | <https://freesound.org/people/Kleber_KGF/sounds/360300/> | CC0 1.0 | Kleber\_KGF（"Drawer Open and Close"，木抽屉推拉；**源文件本身就是 16kHz 单声道**） | 同上，从 **4.995s** 起截 **160ms**（只取抽屉推到底那一下"顿"，不取前面 0.7–2.0s 那段连续摩擦） | 2026-09-14 | 同上（变体 c） |

五条共同的事实，别在表里重复九遍：

- 拿的是 freesound CDN 上的 **ogg 预览件**（`cdn.freesound.org/previews/...`），不是原始 wav ——
  下载原始件要登录，而对一记 80–180ms、最后还要压成 64kbps opus 的接触音来说，
  预览件和原始件之间的差别听不出来。这一条写在这里，免得以后有人以为表里那个 URL 指的是原始件。
- 处理脚本不进仓库（和 `assets/raw/` 同一条规矩）。上面每一行的"做了什么处理"
  已经足够任何人从同一个 URL 重做一遍。
- 授权存证在 `licenses/<文件名>.*`。前四个是 `.png`（下载页截图，2026-09-13 抓的）；
  **这一轮的五个是 `.txt`**：跑这一轮的 lane 上没有浏览器，所以存的是同一个 URL 的
  同一份 HTML 里「License」那一段的**原文**，连同抓取时间与抓取命令。
  它比截图少一件事（页面长什么样），多一件事（**原文可以被 grep 和 diff**）——
  半年后要核对的是"那一页当时写的是不是 CC0"，而那句话在文本里。
  `vite.config.ts` 的 `shipAssets` 把 `licenses/` 排除在 dist 之外 ——
  素材本身 18.9KB，证据 660KB+，让产物为观众永远不会加载的东西变大几十倍是荒唐的。
- CC0 不要求署名。上表照样写了作者，理由见下一节第一条。
  `work-c` 的作者在描述里请求署名（"Just make sure to credit 'Kleber KGF'"）——
  那是**请求不是许可条件**（CC0 1.0 放弃了署名要求），我们照做，但它不改变授权性质。

## 如果要加素材，规矩是

- **只收 CC0 / CC-BY / 公有领域。** CC-BY-NC 一概不收：这件作品可能会进有票务的
  展览，"非商业"那条线不由我们说了算，到时候再来清理素材是最糟的时机。
  CC0 在法律上不要求署名，但**这张表照样要写作者** —— 表是给我们自己看的，不是合规文书。
- **把下载页存下来**放在 `assets/sound/licenses/<文件名>.png`（截图）或
  `<文件名>.txt`（页面原文 + 抓取时间 + 抓取命令，没有浏览器时走这条）。
  素材站的页面会改、条目会被删，**链接不等于证据**。两种形式都行，
  没有存证的文件不许进仓库 —— 这一条不因为"手边没有浏览器"而松。
- **总量不超过 3MB。** 超了就是删掉一个，不是"以后再压"（docs/02 P5）。
- 处理后的文件进仓库，**原始下载件不进**（和 `assets/raw/` 同一条规矩，docs/13 §2）。

## 查过的候选（留痕，不是采购单）

这一轮真去看过、确认授权可用、但**最终没有采用**的方向：

- **freesound.org** — 按 license 筛 `Creative Commons 0` 可以拿到大量 room tone /
  ambient drone。站方明确 CC0 条目可商用且不强制署名（部分作者在描述里请求署名，
  那是请求不是许可条件）。<https://freesound.org/>
- **archive.org** — 公有领域的现场录音与空间 IR。逐条目授权不一致，必须一条一条看。
  <https://archive.org/>
- **CC0 音效库（如 selektaudio 的 CC0 texture 集）** — 打包好的 drone / texture。
  <https://selektaudio.com/sound-lab/textures/drone>

采用其中任何一条**做连续层**之前，先回头读上面"为什么合成"的第 2 条：
**新素材要能接上一个已经存在的信号，否则它只是背景音乐。**
（离散接触音走的是另一条线，见"为什么第五层反过来用素材"。）

## 第五层这一轮试过、听过、**没要**的（留痕）

四记里每一记都不是第一个候选。下面这些是真下载了、真画了波形与频谱、
最后被否掉的，连同否掉的理由 —— 留在这里是为了**别有人回头再试一遍同一个坑**：

- **"陶瓷叮" 一类（freesound 209014 Ceramic Tink / 615308 Ceramic Cup Hit /
  668384 Glass Tap / 262958 Glass Tap）** —— 频谱能量挤在少数几个窄峰上
  （我们量的"窄峰能量占比"分别是 0.44 / 0.77 / 0.80 / 0.96）。
  那就是**一个音高**，也就是"叮！"。这件作品不要成功音。
- **木鱼 / 木块一类（262060 Woodblock Distance / 126481 / 508029）** ——
  干净的单次指数衰减，但周期性极强，听起来是一件**乐器**被敲了一下。
  乐器有调性，调性有情绪，那不是"一次接触"。
- **多记混在一条里的（611501 perc\_woodenfrog 是五记刮奏、371370 / 179997 的马克杯是双击、
  398489 粉笔与 215346 铅笔在 120ms 后各自还有第二下）** ——
  波形上一眼看得见第二个起音。一个动作配一记，配出两记就是在说"发生了两件事"。
- **尾巴不收的（275679 wooden rods、756421 Ceramic Lid）** ——
  截到 70ms 之后仍有一层不衰减的底噪（录音现场的房间）。
  它正好是"为什么合成"第 1 条说的那件事：别人的房间跟着素材一起进来了。
- **能量往上走的（470189 Sliding Paper、496189 Clothing impact、858468 Flipping Page
  Forward）** —— 包络在后半段才到峰值。**上扬 = 成就感**，
  而"确认选中"不是成就；"经过"更不是。
- **塑料 / UI 一类（734219 PlasticMouseClick、575457 Tap Android、342200 Videogame
  Menu BUTTON CLICK）** —— 材质就不对。这件作品的物种是瓷、纸、绒毛、金属。

## 第六层与这一轮那两记，试过、听过、**没要**的（留痕）

同上，真下载了、真量了包络与谱心，最后被否掉的：

- **365863 `Putting a book on a desk`（Jandre160108）** —— 声音本身很对
  （一本硬皮书放到木桌上，正是"触地"想要的那种闷）。**授权是 CC-BY-NC 4.0，
  一票否决**：上面那条"CC-BY-NC 一概不收"就是为这种时刻写的 ——
  它总是在素材最合适的时候出现。
- **670316 `Wood Knock Clean Close`（Geoff-Bremner-Audio）** —— CC-BY 4.0，
  规矩上收得。没要是因为 `work-a/b/c` 已经在 CC0 里凑齐了三个各不相同的变体，
  为一个可替代的变体多欠一份署名义务不划算。留在这里是因为**它是合格的备选**：
  哪天某一个变体要换，先看它。
- **447922 `Thud / Falling on wooden floor`（Breviceps，CC0）** —— 0.39 秒里三个起音
  （它是"摔下去"的全过程，不是一次接触），谱心 4121Hz 偏亮。
  触地要的是**一次**落地，不是一段跌落。
- **481075 `Cotton clothes rustling`（khenshom，CC0）** —— 38 秒连续的布料摩擦。
  它是一段**质感**不是一记**接触**；而且"连续摩擦"那个位置 `pass.webm`（纸）已经占了，
  再加一条布的只会让两者互相打架。
- **559286 的开头（0.03–0.20s）** —— 同一个文件里更响的那一段，但它的峰值落在
  100ms 处，**包络是往上走的**。上扬 = 成就感（见上一节最后一条）。
  所以 `reveal.webm` 取的是 0.120s 之后那一记单独的盒盖接触，不是开头那一对。

一条这一轮学到的：**先看授权再听声音。** 上面第一条是这一轮唯一一次
"听完了才发现用不了"，那十分钟完全可以省掉。
