# 参考图来源 —— `compact`（G1（Unitree））

> 🧒 小人 / 小型 humanoid　·　抓取日期 **2026-09-13**　·　结论：**未找到合规来源**

验收标准是 `docs/30-ASSET-INTAKE.md` §2。下面每一条都写了**过没过、为什么**，
因为图不进版本库（`.gitignore` 有意为之），这份文件就是唯一能重放的记录。

## 收下的图

**没有。** 下面记的是查过的每一条路和它们为什么不行 —— 
`docs/30` §2 说得很清楚，不合格的图不要放进来。`196f527` 那一批就是这么烧掉的。

## 查过的来源（按 `docs/30` 的优先级）

### Unitree G1 产品页

`https://www.unitree.com/g1`

图直链可枚举（`https://www.unitree.com/images/<hash>_<w>x<h>.(jpg|png)`）。已核验两张：`773f6a21…_3840x3600.jpg` 单主体全身正面，但**满屏参数标注文字**，且背景是带烟雾的暗渐变；`87d23aa3…_2000x1470.png` 是**九宫格拼图**——正是 `196f527` 烧过 credits 的那种。两张都不用。

### 同页 800×800 产品小图

`https://www.unitree.com/images/11d0a76afbb74e8fb7f692652b4c33e0_800x800.png`

疑似白底产品图，但 **800px < §2 的 1024 下限**，直接出局。

### Commons

`https://commons.wikimedia.org/wiki/File:Unitree_G1.jpg`

CC0，1380×2326，已核验：展会地毯 + 人群 + 画面里还有一台 Go2。不过。

### 待试

`https://www.unitree.com/images/773f6a21c6764bc8bfd9da5f9d714ecf_3840x3600.jpg`

那张 3840×3600 的标注图，**主体本身是干净的**，文字都在左右两侧留白区。裁掉两侧文字后中间约 1400×3400 的竖条就是一张合格参考图。这是最省事的一条路。

## 怎么再抓一遍

按上面的路线逐条试。**不要为了凑数随便抓一张** —— 
这个条目现在走纯文字 prompt（见同目录 `prompt.md`）是正确的状态，不是缺口。
