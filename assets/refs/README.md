# 参考图 (image-to-3D 的输入)

放法：`assets/refs/<themeId>/*.png|jpg`。
只要某个主题的目录里有图，该主题的所有配方就自动走 **image-to-3D**
（第一张图会被 Rodin 用于材质生成），prompt 退化为方向性引导。

## `_anchor.png` 是自动生成的

如果一个主题**没有**人工参考图，工厂会：

1. 先用 text-to-3D 生成该主题的 `spine.<theme>.a`，并带上 `preview_render=true`，
   Rodin 会额外返回一张官方渲染图；
2. 把这张渲染图存成 `_anchor.png`；
3. 该主题剩下的 19 件全部以这张图作为参考图生成。

好处：**同一主题内的风格一致性由构造保证**，而不是靠 prompt 里的形容词碰运气
（这正是 docs/09 的 U6）。而且整套素材是自洽的，不依赖任何第三方图片。

## 关于放真实产品照片

技术上完全支持：把图丢进 `assets/refs/<theme>/` 就会生效。
但要清楚这意味着什么 —— image-to-3D 的目标就是**还原图里那个物体**。
喂一张某公司机器人的产品照，产出的零件就会是那台机器人的三维复制品，
放进一件公开展出的作品里，这是产品外观的再现，不是"受启发"。

所以默认路径是 `_anchor.png`：**造型语言取材于真实产品，但形体是我们自己的**
（themes.ts 文末的 References 记录了每个主题取材自哪些产品的设计语言）。
要不要放真照片是创作决定，工具这边不拦；只是别在不知情的情况下走到那一步。

自己拍的、自己画的、自己生成的参考图没有这个问题，随便放。

## 2026-09-14 补进仓库的五张 anchor（此前只在本地、没提交）

`char.diva` · `char.line` · `char.painting` · `char.tokusatsu` · `guest.keynote` 这五个条目**已经在 `parts.json` 的索引里**，
选择页要拉它们的 `_anchor.png` —— 文件没进仓库，线上那五张卡片就只能走程序化兜底。作品负责人裁定提交。

| 条目 | 来路 | 画面里是什么 | 可辨认的真人 / 商标 | 瘦身 |
|---|---|---|---|---|
| `char.diva` | 工厂自动生成（Rodin preview render，见上面「`_anchor.png` 是自动生成的」） | 半透明青色长袖连身形，无头无脸 | 无 | 297 KB → 66 KB |
| `char.line` | 同上 | 白色大头小人，黑色描边，无五官 | 无 | 250 KB → 51 KB |
| `char.painting` | 同上 | 一块起皱的布面画框，无人像 | 无 | 594 KB → 120 KB |
| `char.tokusatsu` | 同上 | 银红蓝装甲腿与肩甲，无头无徽标 | 无（泛指特摄装甲的造型语言，不是任何具体角色） | 295 KB → 60 KB |
| `guest.keynote` | 同上 | 一件深灰圆领长袖卫衣，**没有人** | **无**。条目名是「嘉宾」，但图里只有一件衣服；clearance 照 `docs/14 §2` 走，这张图本身不构成肖像 | 402 KB → 105 KB |

提交前逐张看过图（维护者，2026-09-14）。瘦身用 `npm run factory:refs`（768² / 256 色，幂等；同一次还把已提交的 `char.inflate` 压到达标）。
**规矩**：往这个目录加任何一张图，都在这张表里加一行 —— 来路、画面内容、有没有可辨认的人或商标。说不清来路的图不提交。
